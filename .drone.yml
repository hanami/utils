kind: pipeline
name: default

steps:
- name: test
  image: hanami/ruby-2.6
  volumes:
  - name: cache
  - path: /tmp/cache
  commands:
  - bundle install --jobs=3 --retry=3
  - bundle exec rake

volumes:
- name: cache
  host:
    path: /drone/src
